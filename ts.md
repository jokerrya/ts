### (AD)The Specified Domain Either Does Not Exist or Could Not Be Contacted
Active directory requires you to use domain DNS only so replace DNS Servers router address

==========================================================================================
